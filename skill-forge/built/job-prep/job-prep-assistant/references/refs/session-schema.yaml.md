# MD 兼容视图 — `session-schema.yaml`

> **权威源文件**：同目录 `session-schema.yaml`（本文件由 `skill-core` 自动生成，请勿手改；改源文件后重新 `batch build` 或运行 compat 同步。）

| 项 | 值 |
|----|-----|
| 类型 | `yaml` |
| 路径 | `refs/session-schema.yaml` |
| 用途 | session.yaml 字段说明模板。 |

## 内容

```yaml
# session.yaml 字段

```yaml
version: "1.0"
major: cs
target_job: internet-product
matched_job_id: internet-product
skills_installed:
  - resume-tailor
  - behavioral-interview
project_dir: .
host: cursor
scope: project
jd_summary: ""
ethics_ack: true
created_at: ISO8601
```
```
